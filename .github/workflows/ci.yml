name: CI Workflow

on: 
  push:
     branches:
       - main

jobs:
  build:
     runs-on: 
       ubuntu-latest

     steps:
       - name: Checkout Code
       - uses: Run a simple command
  
       -name:Run a simple command
       -run: echo "Hello!"


